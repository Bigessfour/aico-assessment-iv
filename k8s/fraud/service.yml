apiVersion: v1
kind: Service
metadata:
  name: fraud-api
  namespace: fraud
  labels:
    app: fraud-api
    team: fraud
spec:
  type: ClusterIP
  selector:
    app: fraud-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
